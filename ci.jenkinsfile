pipeline{
    agent {
        node {
            label "master || built in"
        }
    }

    stages{
        stage("build"){
            steps{
                script {
                    mvnHome=tool "M2"
                    sh "${mvnHome/bin/mvn -v}"
                }
            }

        }
    }
}